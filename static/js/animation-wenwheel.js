

const message = (whell) => {
  console.log('execute')
  console.log(whell)
}

const drawTriangle = () => {
  //let ctx = this.roulette.ctx;

      // ctx.strokeStyle = "#000"; // Set line colour.
      // ctx.fillStyle = "gold"; // Set fill colour.
      // ctx.lineWidth = 2;
      // ctx.beginPath(); // Begin path.
      // ctx.moveTo(170, 0); // Move to initial position.
      // ctx.lineTo(230, 0); // Draw lines to make the shape.
      // ctx.lineTo(200, 40);
      // ctx.lineTo(171, 0);
      // ctx.stroke(); // Complete the path by stroking (draw lines).
      // ctx.fill(); // Then fill with colour.
      // ctx.restore();
}


